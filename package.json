{
  "name": "sightcue",
  "version": "1.2.0",
  "description": "Real-time vision-based trigger platform with ROI support",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:python": "bash scripts/build-python.sh",
    "build:mac": "npm run build:python && electron-builder --mac && bash scripts/sign-mac.sh 'release/mac-arm64/SightCue.app' && bash scripts/create-dmg.sh",
    "build:win": "electron-builder --win"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5",
    "@mediapipe/tasks-vision": "^0.10.22",
    "@tensorflow/tfjs": "^4.22.0",
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "electron": "^33.0.0",
    "electron-builder": "^26.0.12"
  }
}
